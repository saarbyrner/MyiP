/**
 * Theme exports
 * 
 * This file provides centralized access to the design system theme.
 */

// Re-export the PlaybookThemeProvider
export { PlaybookThemeProvider } from './PlaybookThemeProvider';

// Re-export playbook theme utilities for components that need direct access
export { rootTheme, colors, zIndices } from '@saarbyrne/playbook/themes';
